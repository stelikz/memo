# Components

- Always consider reusability and modularity. Extract repeated UI patterns into this directory.
- Prefer composable, single-responsibility components over large monolithic files.
- Use NativeWind for styling. Keep styles consistent with existing patterns.
